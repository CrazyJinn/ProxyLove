# 04 剧情

> galgame 运行时剧本的**编排设计层**。
> **Choice 选择分叉**已下沉到叙事基础层（[叙事基础.md](叙事基础.md)），作为第 5 种基础实体；本层保留 **Chapter 章节编排 + Section 节编排 + 节级产物链（SecOutline/SecScript/LineAudio）**。
> 叙事图自增长（体检 + 修改建议，支持可选聚焦入参 + 多轮迭代）见 `nrt-narrative-grower` skill，产出落盘 `02_剧情数据/<日期>_建议.json`，由 dashboard 审批写回——既不是图节点，也不写入本层。

---

## 章节编排（Chapter + Section + 产物链）图节点

> galgame 运行时剧本的**编排设计层锚点**。一章（Chapter）由若干节（Section）组成，一节统合若干 Scene。Chapter 只存章级编排信息（标题/序号/全章概要/分支骨架）；Section 是**纯编排容器**（节序/标题/概要，**无 status**），提纲/定稿/配音三种产物各自独立为节点，经 `has_outline`/`produces` 链式相连（sync 级联：改上游自动作废下游）。**对话逐句入图**：定稿（台词.ink，人读/人改的唯一界面格式，ink 方言）审批通过后由 section-voice-publisher 拆分为逐句 `LineAudio` 节点（`SecScript -[:produces {order}]-> LineAudio`，1:N，大间距排序）并配音；行级音频审批状态即行节点 `status`。发布时 publisher 直接从图投影合并各节为章 JSON（运行时唯一消费物），`台词.jsonl` 已停产（历史文件留在创作区，链路不再读取）。

### 章节（Chapter）

最小粒度：一个剧本章节文件对应的**章级编排单元**。结构段在此层完成（统一规划分节 + 全章情感弧）。

| 字段 | 中文 | 类型 | 必填 | 示例 |
|------|------|------|------|------|
| id | 编号 | string | 是 | snowflake Base62 |
| title | 标题 | string | 是 | `新皮肤·Day0` |
| chapter_no | 章节序号 | int | 是 | `1` |
| summary | 概要 | string | 否 | 本章叙事范围一句话（时间/地点/核心事件），编剧的创作纲要 |
| branch_summary | 分支骨架 | string | 否 | 主线/分支/结局简述（如「主线跳江未遂→BE跳下/TE生还」） |
| brief_path | 设计简报路径 | string | 否 | `25_剧本/chapter<NN>_<章概述>/设计简报.md`（structurer 产出；dashboard 结构审渲染简报全文） |
| status | 流程状态 | int | 否 | `0` |

**status 枚举**（章级结构段，一道审批）：`0` 待编排 → `10` 结构待审（structurer 生产完成**直写**，无 submit 步）→ `11` 结构已批。驳回归 `0`，作废 `-1`。规则在 [55_dashboard/core/status.py](../../55_dashboard/core/status.py) 的 `NODE_STATUS` 显式定义（Chapter completion=11）。

> `Chapter.status==11` 只代表「结构已批，进入节级生产」；**章真正可发布 = `Chapter.status==11` AND 全部节产物就绪（各节 SecOutline=1 ∧ SecScript=11 ∧ LineAudio=11）AND 所需立绘全 11**（由 plot-design / dashboard 显式判断，非 Chapter 的 status 字段）。

### 节（Section）

最小粒度：章节内一节对话的**节级编排容器**。一节统合若干 Scene，经 `has_outline` 挂提纲、提纲经 `produces` 链出定稿与配音。**纯编排节点，无 status 字段、无产物路径**（产物挂在 SecOutline/SecScript/LineAudio 三个节点上）。

| 字段 | 中文 | 类型 | 必填 | 示例 |
|------|------|------|------|------|
| id | 编号 | string | 是 | snowflake Base62 |
| section_no | 节序号 | int | 是 | `0`（章内从 0 起；排序权威 + 文件名 `<MM>` 来源） |
| title | 标题 | string | 是 | `酒店清晨` |
| summary | 概要 | string | 否 | 本节戏剧职责（来自 structurer 分节规划） |

### 提纲（SecOutline）

最小粒度：一节的**节级提纲产物节点**（`Section -[has_outline]-> SecOutline`，1:1）。由 `chapter-outliner` 兜底 MERGE 创建并写入。

| 字段 | 中文 | 类型 | 必填 | 示例 |
|------|------|------|------|------|
| id | 编号 | string | 是 | snowflake Base62 |
| name | 名称 | string | 是 | `<节标题>提纲` |
| outline_path | 提纲路径 | string | 否 | `25_剧本/chapter01_新皮肤/sec00_酒店清晨/outline.md`（提纲段产出 Markdown，定稿段读它填 lines） |
| description | 概要 | string | 否 | 简短描述 |
| status | 流程状态 | int | 否 | `0` |

**status 枚举**（数据节点，无审批）：`0` 待提纲 → `1` 提纲就绪。作废 `-1`。`NODE_STATUS` 中 SecOutline completion=1。

### 定稿（SecScript）

最小粒度：一节的**节级定稿产物节点**（`SecOutline -[produces]-> SecScript`，1:1）。由 `chapter-dialoguer` 兜底 MERGE 创建并写入。

| 字段 | 中文 | 类型 | 必填 | 示例 |
|------|------|------|------|------|
| id | 编号 | string | 是 | snowflake Base62 |
| name | 名称 | string | 是 | `<节标题>定稿` |
| script_path | 定稿路径 | string | 否 | `25_剧本/chapter01_新皮肤/sec00_酒店清晨/台词.ink`（人读/人改的唯一界面格式，ink 方言；审批与拆分均以它为准） |
| scene_blocks | 场景块定义 | string(JSON) | 否 | `[{"block":"s01_咖啡店","scene_name":"街角咖啡店-点餐台"}, ...]` 有序数组——场景二级标题的图上落点（scene 行已去图化），拆分时写入；时段投影时按 scene_name JOIN Scene 取 `time_of_day` |
| description | 概要 | string | 否 | 简短描述 |
| status | 流程状态 | int | 否 | `0` |

**status 枚举**（一道定稿审）：`0` 待定稿 → `1` 草稿（dialoguer 草稿模式写，未提审）→ `10` 定稿待审（dialoguer 默认**直写**，无 submit 步）→ `11` 定稿已批。驳回归 `0`，作废 `-1`。**人工微调回路**：用户直接编辑 `台词.ink` 改单句后，dashboard「重新提交审批」`0/1/11→10`（不经 dialoguer，**只改 sc status、不动行节点**）；重批后重拆，text_sha1 匹配的行原样保留审批结果，只有被改句置 `0` 重配——手改不丢。`NODE_STATUS` 中 SecScript completion=11。

### 台词行（LineAudio）

最小粒度：**一句台词/旁白/结构行**（`SecScript -[produces {order}]-> LineAudio`，1:N）。定稿审批通过（SecScript=11）后由 `section-voice-publisher` 第一步「拆分进图」幂等写入（`script_splitter.py` 对齐 台词.ink 与已有行：新增建节点、修改沿用节点置 0、删除 DETACH DELETE、级联作废的未变句恢复）；节点 id（雪花）即**行身份**——voice key 末段，插入/删除行不影响其他行。wav 母带按 `15_声音/<chapter_stem>/<scene_block_id>/<key>.wav` 落盘（key 自身含段信息）。

| 字段 | 中文 | 类型 | 必填 | 示例 |
|------|------|------|------|------|
| id | 编号 | string | 是 | snowflake Base62（= 行身份，voice key 末段） |
| name | 名称 | string | 是 | 行正文本身（= text） |
| op | 行类型 | enum | 是 | `say` / `narrate` / `transition` / `bed_start` / `bed_end` / `label` / `ending`（choice 及配套 jump 暂不进图；场景标题不进行——块定义在 SecScript.scene_blocks，块归属在行上 scene_block_id）。**transition** = 点状音效行（方言 v3 `sfx:` 行，短事件实录 1~2s，**播完再接下一句**）；**bed_start/bed_end** = 音床起止对（`bed+`/`bed-` 行，区间声景**循环播放**，方言解析器保证同块配对） |
| who | 说话人 | string | 否 | say 行必填（Character.name 原名） |
| pos | 立绘位 | enum | 否 | `left` / `center` / `right`（拆分时按块内规则自动分配：块内不同说话人 1 人→`center`、2 人→先说话者 `left` 后说话者 `right`、≥3 人→按首话序 `left`/`right`/`center`；md 不写 pos，规则值即缺省值，重拆自愈对齐） |
| text | 台词原文 | string | 否 | md 拆分写入；投影字幕用；`text_sha1 = sha1(text)` 判「台词改了没」（对齐/stale 唯一依据） |
| tts_text | 配音变体 | string | 否 | 配音期 LLM 由原文产，**仅标点/停顿级修饰**（省略号/叹号/问号/破折号/逗号），**禁增删改汉字**——运行时字幕显示 text，变体发声须与字幕字面一致；**TTS 实际输入与情绪承载唯一通道**（Qwen3 Base clone 无 instruct 通道）；空=未配音；仅 say 行。变体不得写回 text（会破坏对齐）。强烈语气诉求（语气词/引导词）写进台词.ink 原文（text 层，stale 自动重配），不在变体加字 |
| kind | 结局类型 | enum | 否 | **仅 op=ending 行**：`BE` / `TE` / `HE` / `NE` |
| bed | 音床标识 | string | 否 | **仅 op=bed_start/bed_end 行**：`[A-Za-z0-9_]+`，同 scene_block 内 bed+/bed- 配对（方言解析器文件级校验：重复起始/未闭合/跨块/无起始均报错带行号） |
| prompt | 生成提示 | string | 否 | **仅 op=bed_start 行**：AudioFly 英文生成提示（AudioCaps 风格：声源+空间+氛围修饰），产出时写图；重产时 LLM 复用行上已有 prompt（用户要求换措辞才改）。sfx 行不写（Freesound 下载链，登记在 15_声音/sfx_raw/SOURCES.md） |
| scene_block_id | 场景块 | string | 否 | **所有行**：structurer 预分配的块 id（章内唯一），拆分时写入——行上存块归属（不存场景名/时段，块→场景映射见 SecScript.scene_blocks）。新章 ASCII 字母数字下划线（如 `s00_hotel`，**禁连字符**——voice key 解析 rsplit + 标准 ink knot 标识符）；存量 chapter00 中文 id（`s00_酒店` 等）封存保留 |
| voice_key | 配音键 | string | 否 | `<char>-<chapter_stem>-<scene_block_id>-<行id>`，配音后写 |
| ambient_text | （废弃） | string | 否 | 方言 v3（2026-09-03）起内嵌 `【环境音:语义】` 废止（存量已迁移为 bed± 行），解析器拒绝；字段保留仅历史意义（重拆自愈置 null） |
| ambient_track | 环境音键 | string | 否 | **op=transition 行（点状音效）与 op=bed_start 行（音床起）**：`<prefix>-<chapter_stem>-<scene_block_id>-<行id>`（prefix=amb（sfx 与迁移沿用）或 bed（音床新产）；四段制共用解析，**键一经落图终身不换前缀**——重跑 ambient 链沿用行上已有 track 覆盖母带），环境音产出后写；母带 `15_声音/<chapter_stem>/<scene_block_id>/<track>.wav`、运行时 `99_game/assets/sfx/<track>.wav`。章 JSON 投影两型不同：transition 行 → `{"op":"transition","track"}`（运行时**等播完再推进**）；bed 起止 → `{"op":"bed_start","bed","track"}` / `{"op":"bed_end","bed"}`（运行时**循环播放**至 bed_end/换段停） |
| emotion | 情绪 | string | 否 | 配音期判别（12 词表）；图标注与筛选用，不进合成参数（情绪由 tts_text 承载） |
| clone_mode | 克隆模式 | enum | 否 | 配音演绎通道（仅 say 行，配音期判别，**不进章 JSON 投影**——运行时不需要）：`icl`（缺省=ICL，ref codec+ref_text 韵律迁移，音色最稳，平静 ref 韵律会压制文本语气）/ `xvec`（仅说话人向量，丢 ref 韵律，文本语义主导演绎——迟疑/强情绪句用）；bind-graph 写归一化终值（=本句实际合成模式）；人工可在编辑器改，重配时作判别初值 |
| attempts | 尝试次数 | int | 否 | 重配计数；`status=0 且 attempts>0` = 被驳回 |
| text_sha1 | 文本指纹 | string | 否 | sha1(text)，stale 判定依据 |
| status | 流程状态 | int | 否 | `0` |

**status 枚举**（行级音频审——台词文字已在 SecScript 定稿审完成，行 status **只代表音频**）：`0` 待配音/被驳回 → `10` 配完待审（voice-publisher **直写**，无 submit 步）→ `11` 已批。驳回归 `0`，作废 `-1`（sync 级联）。**音频行**（say 配音 / transition 转场音效 / 带 `ambient_text` 的氛围旁白）走 `0→10→11`；**非音频行**（纯 narrate/label/ending）拆分即 `11`。**「节配音完成」**（派生判断）= 该节全部行 status=11；单句驳回由 dashboard 置 0 + deeplink 唤起 plot-design 只重配该句。

> **级联与恢复**：sync 级联（SecScript/上游属性变更）把该节全部行置 `-1`；重拆时 text_sha1 匹配且 wav 在的行恢复 `10`（音频复用不重配，保守进审），否则 `0`。人工微调回路（改 md → 重新提交审批）**不置行 -1**：重拆时未变句原样保留（含已批的 `11`），只有 text 变化句置 `0`——单句修改不丢。order 中点耗尽（上下差 <1）时全节重排，行 id/voice_key 不受影响。

> 创作流程（产物链，由五个 skill 推进）：
> - **结构段·章级**（`chapter-structurer`）：建 Chapter + 规划分节 + 建 Section（纯编排，无 status）+ `has_section`/`contains` 边 + 预分配全章 scene-block id → Chapter `10` 结构待审（直写，无 submit 步）→ dashboard approve `11` 结构已批。
> - **提纲段·节级**（`chapter-outliner`，参数 section_id）：读章级设计简报 + 本节 Scene，产节级提纲 → 兜底建 SecOutline 节点（`Section-[:has_outline]->SecOutline`），写 `outline_path` + `1` 提纲就绪（**无审批**）。
> - **定稿段·节级**（`chapter-dialoguer`，参数 section_id，**纯台词创作**）：读章级简报 + 本节提纲 → 产 `台词.ink`（人读 ink 方言，机器可解析）→ 兜底建 SecScript 节点（`SecOutline-[:produces]->SecScript`），写 `script_path` + `10` 定稿待审 → dashboard approve `11` 定稿已批（人工微调：直接改 ink + 「重新提交审批」0/1/11→10）。
> - **配音段·节级**（`section-voice-publisher`，参数 section_id）：定稿已批（SecScript=11）后**拆分进图**（script_splitter.py 对齐 台词.ink → 逐句 LineAudio 节点 + `produces{order}`）→ 挑行（say 行 status∈{0,-1}）+ 产选绘候选池（portrait_binder candidates）+ **LLM 逐句判别 emotion / clone_mode / 产 tts_text 变体 / 选立绘 stand**（读候选池，为每句 say 行选 StandingIllustration 或提新变体）→ **apply 确定性建边**（`LineAudio-[:uses]->stand` sync=false 每句一条；新变体兜底建 status=0 + depicts/expands_to/ref_style）→ Qwen3 Base Voice Clone 逐句克隆（voice_clone_runner publish）+ bind 写行节点（`voice_key/emotion/tts_text/status=10`）→ dashboard 逐句音频审（行 status 0→10→11）。
> - **BGM·场景级**（`bgm-designer`，**scene-design agent 编排**）：scene-design 发现 Scene 的 BgmTrack 缺口（无节点或 status∈{-1,0}）时调 bgm-designer（自行兜底建 BgmTrack(0)+has_bgm）→ 产音乐描述文字 → 用户手动生成 wav 归档 `13_BGM/<name>.wav` → `2`。plot-design 不查不调 BgmTrack。
> - **发布·章级**（`chapter-publisher`）：全章各节产物就绪（各节 SecOutline=1 ∧ SecScript=11 ∧ 该节全部行 LineAudio=11）时，**从图投影**各节行（produces.order 排序）合并为单一章 JSON（演出注入：立绘整键沿 uses 边投影 + BGM）发布到 `99_game/`。
>
> **节完成**（派生判断，非字段）= SecOutline=1 ∧ SecScript=11 ∧ 该节全部 LineAudio 行=11。

### 边

#### has_section — 章节包含节 `1:N`

- **方向**：`Chapter → Section`

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| sync | 同步 | boolean | 否 | 默认 `true`（组成关系：章愿景变更 → 节作废重做） |

> Section 是 Chapter 的独占组成（一节只属一章）。人改 Chapter 属性时，沿 `sync=true` 出边把所有可达 Section 及其产物链（SecOutline→SecScript→LineAudio）重置为 `-1`（作废重做）。Section 的排序权威是 `section_no` 字段，不在边上另存 order。

#### has_outline — 节拥有提纲 `1:1`

- **方向**：`Section → SecOutline`

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| sync | 同步 | boolean | 否 | 默认 `true`（节编排属性变更 → 提纲及其下游全作废重做） |

> 一节一份提纲，由 `chapter-outliner` 兜底 MERGE 创建。编辑已就绪的 SecOutline 属性时，沿 `produces` 链把 SecScript/LineAudio 重置为 `-1`（改提纲 → 定稿/配音作废）。

#### produces — 提纲产出定稿 `1:1` / 定稿产出台词行 `1:N`

- **方向**：`SecOutline → SecScript`（1:1）、`SecScript → LineAudio`（**1:N**，逐句行）

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| order | 排序 | int | 否 | **仅 SecScript→LineAudio 段**：行在节内的顺序。大间距数字——初始 `(i+1)×1000`；两句之间插入新行取 `(上句 order + 下句 order) ÷ 2`（整除）；中点差 <1 时全节重排（order 仅为排序键不进 voice key，重排安全） |
| sync | 同步 | boolean | 否 | 默认 `true`（上游产物变更 → 下游作废重做） |

> 链式级联：`SecOutline -[:produces]-> SecScript -[:produces]-> LineAudio(×N)`。改提纲 → 定稿 `-1` + 全部行 `-1`；改定稿 → 全部行 `-1`（重拆时 text_sha1 匹配且 wav 在的行恢复 `10`，只重配被改句）。**审批互不牵连**：驳回 SecScript 只重做 SecScript，SecOutline 不动；行级驳回只置该行 `0`。

> **stages 边已废止**（2026-08-29 scene 行去图化）：场景块不再建 LineAudio(op=scene) 行节点——块定义存 `SecScript.scene_blocks`，块归属存各行的 `scene_block_id`；块→场景实体的映射经 scene_blocks 的 scene_name 投影时 JOIN Scene（时段读 `Scene.time_of_day`）。场景美术链（has_bgm/depicts）以 Scene 为锚点，本就不经行节点。

#### contains — 节包含场景 `N:M`

- **方向**：`Section → Scene`

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| order | 序号 | int | 否 | 该场景在本节 scene-block 序列中的首次出现位置（JSON 的完整序列为运行时权威） |
| sync | 同步 | boolean | 否 | 默认 `false`（编排引用，不级联） |

> 节编排引用其用到的 Scene（Scene 已由 `Location -[has_scene]-> Scene` 存在）。一个 Scene 可被多个 Section / 多个 Chapter 引用。同一 Scene 在一节内重复出现时 MERGE 合为一条边，完整序列以图行（produces.order + op=scene 行的 scene_block_id）/章 JSON 为权威。

#### depicts — 场景引用立绘设计图 `N:N`

- **方向**：`Scene → IllusDesign`

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| sync | 同步 | boolean | 否 | 默认 `false`（引用边，不级联） |

> 表示「该场景的剧本需要此着装的立绘可用」。由 `section-voice-publisher` 选绘应用步骤（portrait_binder.py apply）在为台词行绑定立绘时顺带 MERGE（sync=false，同 Scene 同 IllusDesign 去重）；候选池解析时按「场景事件 wears 优先、兜底 has_costume」选定 IllusDesign 后补建。**变体级缺口跟踪走 expands_to**：选绘 LLM 判定池中无贴切变体时，兜底建 `StandingIllustration(status=0)` + `IllusDesign -[:expands_to]-> stand` + `LanguageStyle -[:ref_style]-> stand`（description 写变体氛围）。同一 IllusDesign 被多 Scene 引用时变体池共用（出图按 IllusDesign 级一次出多场景用）。`plot-design` agent 沿 `Scene -[:depicts]-> IllusDesign -[:expands_to]-> stand` 两跳枚举变体，对每个 `status≠11` 的 stand 直调 `char-stand-designer <stand_id>` 推进出图——立绘调用已从 `char-design` 剥离至 `plot-design`（按需出图，避免为未被引用的变体浪费 API）。

#### uses — 台词行使用立绘 `1:N`（每 say 行至多 1 条）

- **方向**：`LineAudio → StandingIllustration`

| 属性 | 中文 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| sync | 同步 | boolean | 否 | 固定 `false`（**行级演出引用，禁止级联**——立绘资产被多句共享，行属性编辑/作废不得沿边作废立绘） |

> 配音判断期由 `section-voice-publisher` 的 LLM 选绘产生（与 emotion/clone_mode/tts_text 同一次逐句判断，tasks 项 `stand` 字段），由 portrait_binder.py `apply` 确定性建边（先删该行旧 uses 再 MERGE，幂等替换）。选绘范围与挑行一致：`say 且 status∈{0,-1}`（被改句置 0 后自然重判）。**每句 say 行显式选绘**（即使与上一句相同也各自建边）——say 行无 uses 边 = 明确缺口，发布期（merge_sections_to_chapter.py）打警告、章 JSON 该句 portrait 为空串（运行时占位图兜底）。发布投影沿 `uses→expands_to→outfit_for` 解析整键 `<char>-<costume>-<variant>-<stand_id>`（缺着装孤儿立绘为三段键，与 portrait_key.make_key 同源）。新变体兜底建时写 `StandingIllustration.description`（变体氛围，作者=选绘 LLM）。
